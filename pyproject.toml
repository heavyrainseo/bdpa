[project]
name = "bdpa"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Daewoo, Seo", email = "hvyrain@ysc.ac.kr" }
]
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.11.2",
    "pandas>=3.0.6",
    "plotly>=7.1.0",
    "seaborn>=0.13.2",
    "streamlit>=1.64.0",
    "koreanize-matplotlib",
]

[project.scripts]
bdpa = "bdpa:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
